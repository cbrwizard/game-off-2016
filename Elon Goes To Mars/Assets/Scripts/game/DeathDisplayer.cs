using UnityEngine;

/**
  Is responsible for displaying objects when a player dies.
  Maybe not needed.
**/
public class DeathDisplayer: MonoBehaviour {
  // We can handle it!
  public void HandleDeath()
  {
    Debug.Log("handle!");
  }
}
